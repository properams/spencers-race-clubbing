# clean/

Genormaliseerde GLB's, geproduceerd door `pipeline/optimize.mjs`
(weld → dedup → simplify → pivot-to-foot → meshopt → KTX2). Niet handmatig
bewerken; de pipeline houdt `atelier/manifest.json` bij.
